# 本地数据库数据源实现总结

本文档记录将本地数据库添加为新数据源的完整实现过程。

---

## 修改概览

本次修改将本地已导入的K线数据库作为新的数据源，实现了数据源的完整集成，包括：
- 数据源类型定义
- 数据源适配器实现
- 数据源能力描述
- 数据源路由配置
- 数据源协议详情

---

## 修改文件清单

### 前端文件

#### 1. DataSourceConfig.vue
**文件路径**: `javaTdx/frontend/src/views/DataSourceConfig.vue`

**修改内容**:

| 修改项 | 说明 |
|--------|------|
| dataSourceStatus | 添加本地数据库到数据源状态列表，优先级设为0（最高） |
| fallbackStrategies | 在realtime、kline、history策略中添加本地数据库作为第一优先级 |
| matrixSources | 添加本地数据库到能力矩阵数据源列表 |
| capabilityMatrix | 为所有字段添加本地数据库支持标记(0/1/2) |
| getSourceIcon() | 添加 LOCAL_DATABASE 图标映射 (el-icon-s-grid) |
| getSourceDisplayName() | 添加 LOCAL_DATABASE 显示名称映射 |
| getProtocolIcon() | 添加 LOCAL_DATABASE 协议图标映射 |
| testStockCode | 添加 LOCAL_DATABASE 测试股票代码 |

**对应页面**: 数据源配置页面 (`/data-source-config`)

---

#### 2. DataSourceModeConfig.vue
**文件路径**: `javaTdx/frontend/src/views/DataSourceModeConfig.vue`

**修改内容**:

| 修改项 | 说明 |
|--------|------|
| supportedModes | 添加 LOCAL_DATABASE 到支持的模式列表 |
| modeNameMap | 添加 LOCAL_DATABASE 模式名称映射 |
| sourceNameMap | 添加 LOCAL_DATABASE 数据源名称映射 |
| sourceConfigs | 添加 LOCAL_DATABASE 基础配置 (batchSize: 1000) |
| 模式选择按钮 | 添加"本地数据库"单选按钮 |
| isSingleSourceMode | 将 LOCAL_DATABASE 添加到单数据源模式判断 |

**对应页面**: 数据源模式配置页面 (`/data-source-mode`)

---

### 后端文件

### 1. DataSourceType.java
**文件路径**: `javaTdx/src/main/java/com/example/tdx/service/datasource/DataSourceType.java`

**修改内容**: 添加 LOCAL_DATABASE 枚举值

```java
/**
 * LOCAL_DATABASE - 本地数据库数据源
 */
LOCAL_DATABASE("LOCAL_DATABASE", "本地数据库", "本地已导入的K线数据", false, 0);
```

**对应接口**: 数据源类型定义，用于系统识别和区分不同数据源

---

### 2. LocalDatabaseDataSourceAdapter.java (新增)
**文件路径**: `javaTdx/src/main/java/com/example/tdx/service/datasource/LocalDatabaseDataSourceAdapter.java`

**修改内容**: 创建本地数据库数据源适配器，实现 StockDataSource 接口

**核心方法**:

| 方法名 | 说明 | 对应接口 |
|--------|------|----------|
| `getSourceName()` | 返回"本地数据库(已导入数据)" | 数据源名称接口 |
| `getSourceType()` | 返回 DataSourceType.LOCAL_DATABASE | 数据源类型接口 |
| `isAvailable()` | 检查本地数据库是否有数据 | 数据源可用性检查接口 |
| `getPriority()` | 返回最高优先级 5 | 数据源优先级接口 |
| `getCapability()` | 返回本地数据库能力描述 | 数据源能力接口 |
| `collect(String stockCode)` | 从本地数据库采集单只股票数据 | 单条数据采集接口 |
| `collectBatch(List<String>)` | 批量采集多只股票数据 | 批量数据采集接口 |
| `getWeight()` | 返回最高权重 100 | 数据源权重接口 |
| `getHealth()` | 返回数据源健康状态 | 数据源健康检查接口 |

**支持的字段**:
- currentPrice (最新价) - stock_kline.close_price
- openPrice (开盘价) - stock_kline.open_price
- highPrice (最高价) - stock_kline.high_price
- lowPrice (最低价) - stock_kline.low_price
- previousClose (昨日收盘价) - 前一根K线的收盘价
- volume (成交量) - stock_kline.volume
- turnover (成交额) - stock_kline.amount
- yesterdayChange (昨日涨幅) - 计算得出
- threeDayChange (3日涨幅) - 计算得出
- fiveDayChange (5日涨幅) - 计算得出
- tenDayChange (10日涨幅) - 计算得出
- twentyDayChange (20日涨幅) - 计算得出
- sixtyDayChange (60日涨幅) - 计算得出

**对应接口**: 完整的数据源适配器接口实现

---

### 3. DataSourceCapability.java
**文件路径**: `javaTdx/src/main/java/com/example/tdx/service/datasource/DataSourceCapability.java`

**修改内容**: 添加 localDatabaseCapability() 静态方法

```java
/**
 * 获取本地数据库数据源能力
 */
public static DataSourceCapability localDatabaseCapability() {
    Set<String> supportedFields = new HashSet<>();
    supportedFields.add("stockCode");
    supportedFields.add("market");
    supportedFields.add("currentPrice");
    supportedFields.add("previousClose");
    supportedFields.add("openPrice");
    supportedFields.add("highPrice");
    supportedFields.add("lowPrice");
    supportedFields.add("volume");
    supportedFields.add("turnover");
    supportedFields.add("yesterdayChange");
    supportedFields.add("threeDayChange");
    supportedFields.add("fiveDayChange");
    supportedFields.add("tenDayChange");
    supportedFields.add("twentyDayChange");
    supportedFields.add("sixtyDayChange");

    return DataSourceCapability.builder()
            .supportRealTimeQuote(false)
            .supportOrderBook(false)
            .supportHistoricalData(true)
            .historicalDataDays(3650)  // 10年历史数据
            .supportFinancialData(false)
            .supportIndustry(false)
            .supportConcept(false)
            .supportValuation(false)
            .supportMarketCap(false)
            .supportTurnoverRate(false)
            .supportLimitUpStats(false)
            .supportedFields(supportedFields)
            .updateFrequency(86400)  // 每日更新
            .dailyLimit(Integer.MAX_VALUE)  // 无限制
            .minuteLimit(Integer.MAX_VALUE)  // 无限制
            .supportBatchQuery(true)
            .maxBatchSize(1000)
            .build();
}
```

**对应接口**: 数据源能力描述接口

---

### 4. DataSourceRouter.java
**文件路径**: `javaTdx/src/main/java/com/example/tdx/service/datasource/DataSourceRouter.java`

**修改内容1**: 更新 initFieldPriority() 方法，将本地数据库设为最高优先级

```java
// 实时行情字段 - 本地数据库优先（响应快），然后免费数据源
fieldPriorityMap.put("currentPrice", Arrays.asList(
        DataSourceType.LOCAL_DATABASE,  // 第一优先级
        DataSourceType.FREE,
        DataSourceType.TUSHARE_BASIC,
        DataSourceType.TUSHARE_PRO
));
// ... 其他字段类似，LOCAL_DATABASE 均为第一优先级
```

**修改内容2**: 更新 getDataSourceProtocols() 方法，优先展示本地数据库

```java
public List<DataSourceProtocolDTO> getDataSourceProtocols() {
    List<DataSourceProtocolDTO> protocols = new ArrayList<>();

    // 添加本地数据库数据源（首先展示）
    if (dataSources != null) {
        for (StockDataSource source : dataSources) {
            if (source == null) continue;
            DataSourceType type = source.getSourceType();
            if (type == DataSourceType.LOCAL_DATABASE) {
                protocols.add(buildLocalDatabaseProtocolDTO(source));
            }
        }
    }
    // ... 其他数据源
}
```

**修改内容3**: 新增 buildLocalDatabaseProtocolDTO() 方法

```java
private DataSourceProtocolDTO buildLocalDatabaseProtocolDTO(StockDataSource source) {
    return DataSourceProtocolDTO.builder()
            .sourceName(source.getSourceName())
            .sourceType("LOCAL_DATABASE")
            .available(source.isAvailable())
            .description("本地数据库数据源 - 使用已导入的K线数据，响应速度最快")
            .protocolType("本地数据库")
            .baseUrl("JDBC/H2")
            .authType("无需认证")
            .requireToken(false)
            .dataFormat("关系型数据库")
            // ... 字段映射和API端点
            .build();
}
```

**对应接口**: 
- 数据源路由选择接口
- 数据源协议详情查询接口 (`GET /api/data-source/protocols`)

---

## 数据源特性对比

| 特性 | 本地数据库 | 新浪财经 | 东方财富 | MOOTDX |
|------|-----------|----------|----------|--------|
| **响应速度** | ⭐⭐⭐⭐⭐ 最快 | ⭐⭐⭐ 中等 | ⭐⭐⭐ 中等 | ⭐⭐ 较慢 |
| **实时性** | ❌ 非实时(T-1) | ✅ 实时 | ✅ 实时 | ✅ 实时 |
| **历史数据** | ⭐⭐⭐⭐⭐ 10年 | ❌ 不支持 | ⭐⭐ 部分 | ⭐⭐⭐ 3年 |
| **调用限制** | 无限制 | 高频限制 | 高频限制 | 服务器限制 |
| **网络依赖** | ❌ 无需网络 | ✅ 需要 | ✅ 需要 | ✅ 需要 |
| **数据稳定性** | ⭐⭐⭐⭐⭐ 高 | ⭐⭐⭐ 中 | ⭐⭐⭐ 中 | ⭐⭐ 低 |
| **支持字段** | 基础行情+涨跌幅 | 完整行情 | 完整行情 | 完整行情 |
| **五档盘口** | ❌ 不支持 | ✅ 支持 | ❌ 不支持 | ✅ 支持 |

---

## 字段映射详情

### 本地数据库字段映射

| 系统字段 | 数据库字段 | 说明 | 计算方式 |
|----------|-----------|------|----------|
| currentPrice | stock_kline.close_price | 最新价 | 直接读取 |
| openPrice | stock_kline.open_price | 开盘价 | 直接读取 |
| highPrice | stock_kline.high_price | 最高价 | 直接读取 |
| lowPrice | stock_kline.low_price | 最低价 | 直接读取 |
| previousClose | 前一根K线.close_price | 昨日收盘价 | 直接读取 |
| volume | stock_kline.volume | 成交量 | 直接读取 |
| turnover | stock_kline.amount | 成交额 | 直接读取 |
| yesterdayChange | - | 昨日涨幅 | (最新-昨日)/昨日*100 |
| threeDayChange | - | 3日涨幅 | (最新-3日前)/3日前*100 |
| fiveDayChange | - | 5日涨幅 | (最新-5日前)/5日前*100 |
| tenDayChange | - | 10日涨幅 | (最新-10日前)/10日前*100 |
| twentyDayChange | - | 20日涨幅 | (最新-20日前)/20日前*100 |
| sixtyDayChange | - | 60日涨幅 | (最新-60日前)/60日前*100 |

---

## API 端点

### 1. 数据源协议详情查询

**接口地址**: `GET /api/data-source/protocols`

**响应示例**:
```json
[
  {
    "sourceName": "本地数据库(已导入数据)",
    "sourceType": "LOCAL_DATABASE",
    "available": true,
    "description": "本地数据库数据源 - 使用已导入的K线数据，响应速度最快",
    "protocolType": "本地数据库",
    "baseUrl": "JDBC/H2",
    "authType": "无需认证",
    "requireToken": false,
    "dataFormat": "关系型数据库",
    "updateFrequency": 86400,
    "dailyLimit": 2147483647,
    "minuteLimit": 2147483647,
    "supportedFields": ["stockCode", "market", "currentPrice", ...],
    "fieldMappings": { ... },
    "apiEndpoints": [ ... ]
  },
  // ... 其他数据源
]
```

### 2. 数据源路由报告

**接口地址**: `GET /api/data-source/routing-report`

**响应示例**:
```json
{
  "availableSources": ["本地数据库(已导入数据)", "新浪财经", "东方财富", ...],
  "fieldCoverage": {
    "currentPrice": "LOCAL_DATABASE",
    "yesterdayChange": "LOCAL_DATABASE",
    "threeDayChange": "LOCAL_DATABASE",
    // ... 本地数据库为多个字段的第一优先级
  }
}
```

---

## 使用场景

### 适用场景
1. **历史数据分析** - 需要查询大量历史K线数据
2. **批量股票筛选** - 对全市场股票进行指标计算
3. **离线环境** - 无网络连接时的数据查询
4. **高并发查询** - 避免频繁请求外部API
5. **数据备份验证** - 验证已导入数据的完整性

### 不适用场景
1. **实时交易** - 数据非实时(T-1)
2. **五档盘口** - 不支持买卖盘数据
3. **财务数据** - 不支持财务报表数据
4. **行业概念** - 不支持行业和概念数据

---

## 配置说明

### 自动配置
本地数据库数据源通过 Spring 的 `@Service` 注解自动注册，无需额外配置。

### 优先级配置
数据源优先级已在 `DataSourceRouter.initFieldPriority()` 中配置：
- 所有基础行情字段：LOCAL_DATABASE > FREE > TUSHARE_BASIC > TUSHARE_PRO

### 可用性检查
系统自动检查本地数据库是否有数据：
- 有数据：数据源标记为可用
- 无数据：数据源标记为不可用，提示"请先导入数据"

---

## 测试验证

### 1. 编译测试
```bash
mvn clean compile -DskipTests
# 结果: BUILD SUCCESS
```

### 2. 功能测试步骤
1. 启动后端服务
2. 访问数据源协议详情页面: `GET /api/data-source/protocols`
3. 确认"本地数据库(已导入数据)"显示在首位
4. 导入一些K线数据
5. 验证数据源状态变为可用
6. 测试股票数据查询，确认优先使用本地数据库

---

## 性能优化

### 数据库查询优化
- 使用 `findTop500ByCodeAndMarketAndFreqOrderByTradeDateDescTradeTimeDesc` 方法
- 限制返回500条K线数据，避免内存溢出
- 按交易日期和时间倒序排列，确保最新数据在前

### 缓存策略
- 本地数据库查询结果可配合 Spring Cache 使用
- 建议对常用股票的历史数据进行缓存

---

## 后续优化建议

1. **增量更新** - 实现定时任务，自动从通达信更新最新数据
2. **数据压缩** - 对历史K线数据进行压缩存储，减少磁盘占用
3. **索引优化** - 为常用查询字段添加数据库索引
4. **分库分表** - 数据量增大时考虑按股票代码分表
5. **缓存预热** - 系统启动时预加载热门股票数据到缓存

---

## 总结

本次实现完整地将本地数据库集成到数据源系统中，实现了：

### 后端实现
✅ **完整的数据源接口实现** - LocalDatabaseDataSourceAdapter  
✅ **最高优先级配置** - 所有相关字段的第一优先级  
✅ **详细的能力描述** - 支持字段、限制、特性完整定义  
✅ **协议详情展示** - 在数据源协议页面完整展示  
✅ **健康状态检查** - 自动检测数据可用性  
✅ **编译通过** - 代码质量符合项目标准  

### 前端实现
✅ **数据源状态展示** - 在数据源配置页面显示本地数据库状态  
✅ **降级策略配置** - 本地数据库作为第一优先级  
✅ **能力矩阵展示** - 显示本地数据库支持的字段  
✅ **模式配置支持** - 支持本地数据库模式选择和配置  
✅ **图标和名称映射** - 统一的视觉呈现  

本地数据库数据源的优势在于**响应速度快**、**无调用限制**、**无需网络**，非常适合历史数据分析和批量计算场景。
