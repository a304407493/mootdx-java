# 用户原话记录规范（AI执行指令）- 优化版

## 任务说明
将用户在本对话中所说的**所有话**记录到 Markdown 文档中。

---

## ❌ 常见错误案例（必须避免）

### 错误1：时间范围限制
**错误做法**：只记录"今天"或"最近"的对话  
**正确做法**：从会话的**第一条消息**开始记录，无论多久之前

### 错误2：遗漏用户输入
**错误做法**：只记录主要指令，忽略简短回复（如"继续"、"好的"）  
**正确做法**：**每一个** `<user_input>` 标签内的内容都要记录

### 错误3：时间戳不准确
**错误做法**：随意估算时间  
**正确做法**：使用对话中显示的实际时间，精确到分钟

### 错误4：覆盖已有文件
**错误做法**：直接使用 `Write` 工具写入已有文件  
**正确做法**：先 `Read` 读取，再用 `SearchReplace` 追加

---

## 执行步骤（必须按顺序执行）

### 步骤0：文件存在性检查（执行任何操作前）
- **必须先检查文件是否存在**
- 如果文件存在 → 使用 `Read` 工具读取现有内容
- 如果文件不存在 → 才能使用 `Write` 创建新文件
- **禁止直接对已存在文件使用 `Write` 工具覆盖**

### 步骤1：遍历完整对话历史（关键步骤）
- **必须从头到尾遍历整个对话历史**，不能只看最近几条
- **从会话的第一条消息开始**，不是从"今天"开始
- 识别所有 `<user_input>` 标签内的内容
- 包括用户最早说的话，不能遗漏任何一句
- ⚠️ **特别注意**：简短的回复如"继续"、"好的"、"嗯"也要记录

### 步骤2：提取用户原话
- **提取用户输入的所有内容**，不包括AI的回复
- **一个字和标点符号都不能改**，完全保持原样
- 超长的日志内容可以省略，但用户的话必须完整保留

### 步骤3：添加时间戳
- 为用户的每句话添加时间戳
- 时间格式：`YYYY-MM-DD HH:MM`
- 时间精确到**分钟**
- 如果无法确定具体时间，使用对话中的时间标记

### 步骤4：文件命名规则
- 文件名格式：`用户原话_YYYY-MM-DD.md`
- 如果文件已存在，**追加内容**，不要覆盖或删除原有内容
- 追加时使用分隔线 `---` 分隔不同时间段的内容

### 步骤5：文件内容格式
```markdown
# 用户原话记录

## YYYY-MM-DD HH:MM

用户说的第一句话

用户说的第二句话

---

## YYYY-MM-DD HH:MM

用户之后说的话（追加内容）
```

---

## 防止遗漏检查清单（执行后必须逐项核对）

- [ ] **是否先检查了文件是否存在？**
- [ ] **如果文件存在，是否先读取了现有内容？**
- [ ] **是否使用了追加/编辑而不是覆盖？**
- [ ] **是否查看了对话历史的第一条消息？**（从会话开始，不是从今天）
- [ ] **是否查看了对话历史的最后一条消息？**
- [ ] **是否遗漏了任何 `<user_input>` 内容？**（包括"继续"、"好的"等简短回复）
- [ ] **所有用户说的话是否都已记录？**
- [ ] **时间戳是否都已添加？**

---

## 工具使用规则

### 写入文件时：
1. **首次创建** → 使用 `Write` 工具
2. **已有文件追加** → 使用 `Read` 读取 → 使用 `SearchReplace` 在末尾追加
3. **禁止直接覆盖** → 绝不在已有文件上使用 `Write`

### 追加内容的标准操作：
```python
# 1. 先读取现有文件
Read(file_path="...")

# 2. 使用 SearchReplace 在文件末尾追加
SearchReplace(
    file_path="...",
    old_str="文件原有最后内容",
    new_str="文件原有最后内容\n\n---\n\n## YYYY-MM-DD HH:MM\n\n新内容"
)
```

---

## ❌ 错误示例（绝对禁止）
```python
Write(file_path="用户原话_2026-03-18.md", content="新内容")
# 结果：原有内容被完全删除！这是严重错误！
```

## ✅ 正确示例（必须这样做）
```python
# 第一步：先读取现有内容
Read(file_path="用户原话_2026-03-18.md")

# 第二步：在末尾追加新内容
SearchReplace(
    file_path="用户原话_2026-03-18.md",
    old_str="## 2026-03-18 16:17\n\n`提示词.md` 根据文档执行下",
    new_str="## 2026-03-18 16:17\n\n`提示词.md` 根据文档执行下\n\n---\n\n## 2026-03-18 17:03\n\n新的用户原话"
)
```

---

## 注意事项
- ❌ 不要修改用户的任何文字
- ❌ 不要删除文件已有内容
- ❌ 不要包含AI的回复内容
- ❌ **不要遗漏任何用户说的话，包括最早的对话**
- ❌ **禁止对已存在文件使用 Write 工具**
- ✅ 必须保留所有标点符号
- ✅ 必须保留原始格式
- ✅ 相同日期的文件必须追加（使用 SearchReplace）
- ✅ **必须遍历完整的对话历史，从头到尾**
- ✅ **简短的回复也要记录（如"继续"、"好的"、"嗯"）**
