# Mootdx-Java 策略实现总结

> 生成日期: 2026-05-12

---

## 一、数据源选择策略

**文件位置**: [StockDataService.java](file:///d:/BaiduNetdiskDownload/mootdx-java/javaTdx/src/main/java/com/example/tdx/service/StockDataService.java)

### 1.1 主入口方法

`getStockInfo(List)` (L44-L86):

```java
public List<StockInfoDTO> getStockInfo(List<String> stockCodes) {
    String sourceType = dataSourceModeConfig.getDefaultSource();
    
    switch (sourceType.toUpperCase()) {
        case "TENCENT":
            return unifiedDataSourceService.getStockInfo("TENCENT", stockCodes);
        case "SINA":
            return unifiedDataSourceService.getStockInfo("SINA", stockCodes);
        case "EASTMONEY":
            return unifiedDataSourceService.getStockInfo("EASTMONEY", stockCodes);
        case "MOOTDX":
            return unifiedDataSourceService.getStockInfo("MOOTDX", stockCodes);
        case "FREE":
            return getStockInfoFromFreeCollectorWithMode(stockCodes);
        case "FALLBACK":
            return fallbackStrategyService.getStockInfo(stockCodes);
        case "COMBINED":
        case "AGGREGATED":
            return getStockInfoFromAggregatedWithMode(stockCodes, CollectionStrategy.MOST_COMPLETE);
        case "FAST":
            return getStockInfoFromAggregatedWithMode(stockCodes, CollectionStrategy.FASTEST);
        default:
            return getStockInfoFromAggregatedWithMode(stockCodes, CollectionStrategy.SMART);
    }
}
```

### 1.2 策略选择说明

| 数据源类型 | 使用服务 | 说明 |
|-----------|----------|------|
| TENCENT | UnifiedDataSourceService | 腾讯财经数据源 |
| SINA | UnifiedDataSourceService | 新浪财经数据源 |
| EASTMONEY | UnifiedDataSourceService | 东方财富数据源 |
| MOOTDX | UnifiedDataSourceService | 通达信本地数据源 |
| FREE | FreeDataCollectorService | 免费数据源组合 |
| FALLBACK | FallbackStrategyService | 降级策略 |
| AGGREGATED | AggregatedDataCollectorService | 多源融合-最全策略 |
| FAST | AggregatedDataCollectorService | 多源融合-最快策略 |
| default | AggregatedDataCollectorService | 多源融合-智能策略 |

---

## 二、查询模式策略

### 2.1 AUTO模式逻辑

**自动选择逻辑** (StockDataService.java:L200-L210):

```java
// AUTO模式：根据股票数量自动选择
if (stockCodes.size() <= 10) {
    // 股票数量 <= 10，使用单笔查询
    return getStockInfoFromAggregatedSingle(stockCodes, strategy);
} else {
    // 股票数量 > 10，使用批量查询
    return getStockInfoFromAggregatedBatch(stockCodes, strategy);
}
```

### 2.2 模式说明

| 模式 | 说明 | 适用场景 |
|------|------|----------|
| BATCH | 批量查询 | 股票数量多，追求效率 |
| SINGLE | 单笔查询 | 股票数量少，追求成功率 |
| AUTO | 自动选择 | <=10只使用SINGLE，>10只使用BATCH |

---

## 三、降级策略执行流程

**文件位置**: [FallbackStrategyService.java](file:///d:/BaiduNetdiskDownload/mootdx-java/javaTdx/src/main/java/com/example/tdx/service/datasource/FallbackStrategyService.java)

### 3.1 执行流程 (L46-L86)

1. **获取配置**: 从DataFetchConfig获取mode和priority
2. **模式选择**: 
   - SINGLE -> 执行单笔查询
   - BATCH -> 执行批量查询  
   - AUTO -> 根据股票数量自动选择
3. **数据源尝试**: 按优先级顺序尝试各数据源
4. **成功返回**: 任一数据源成功即返回
5. **失败处理**: 所有数据源失败返回空结果

### 3.2 批量查询流程 (L91-L155)

```
对于每个数据源:
  1. 获取批量大小配置
  2. 分批查询（批次间延迟）
  3. 成功则计算派生字段并返回
  4. 失败则尝试下一个数据源
```

### 3.3 单笔查询流程 (L180-L269)

```
对于每只股票:
  1. 按优先级尝试各数据源
  2. 记录API调用次数
  3. 成功获取则跳出循环
  4. 所有数据源失败创建空对象
  5. 单笔延迟后继续下一只
```

### 3.4 默认优先级

```
SINA > TENCENT > MOOTDX > EASTMONEY
```

---

## 四、多源融合策略

**文件位置**: [AggregatedDataCollectorService.java](file:///d:/BaiduNetdiskDownload/mootdx-java/javaTdx/src/main/java/com/example/tdx/service/datasource/AggregatedDataCollectorService.java)

### 4.1 采集策略

```java
public enum CollectionStrategy {
    FASTEST,       // 最快优先 - 使用第一个返回的数据
    MOST_COMPLETE, // 最全优先 - 合并所有数据源的数据
    FREE_FIRST,    // 免费优先 - 优先使用免费数据源
    SMART          // 智能路由 - 根据字段需求自动选择
}
```

### 4.2 最快优先策略 (L119-L154)

```
1. 并行采集所有可用数据源
2. 获取第一个成功的结果
3. 超时时间为5秒
4. 计算派生字段后返回
```

### 4.3 最全优先策略 (L159-L204)

```
1. 并行采集所有可用数据源
2. 等待所有采集完成（超时10秒）
3. 使用DataMergeService合并数据
4. 按字段优先级选择最佳值
```

### 4.4 免费优先策略 (L206-L226)

```
1. 首先尝试免费数据源
2. 失败则使用最快优先策略
```

### 4.5 智能策略 (L228-L234)

```
默认使用最全优先策略
```

---

## 五、数据合并策略

**文件位置**: [DataMergeService.java](file:///d:/BaiduNetdiskDownload/mootdx-java/javaTdx/src/main/java/com/example/tdx/service/datasource/DataMergeService.java)

### 5.1 字段优先级配置 (L25-L165)

| 字段类别 | 优先级顺序 |
|----------|-----------|
| 实时行情(currentPrice等) | FREE > TUSHARE_BASIC > TUSHARE_PRO |
| 五档盘口(buy1Price等) | 仅FREE |
| 估值指标(peRatio等) | FREE > TUSHARE_PRO |
| 历史涨跌幅(yesterdayChange等) | TUSHARE_BASIC > TUSHARE_PRO > FREE |
| 财务数据(netProfitAfterDeduction) | 仅TUSHARE_PRO |
| 行业分类(subIndustry) | TUSHARE_BASIC > TUSHARE_PRO > FREE |

### 5.2 合并流程 (L170-L210)

```
1. 创建新的StockInfoDTO对象
2. 遍历所有字段
3. 根据fieldPriority选择最佳值
4. 计算派生字段
5. 返回合并后的对象
```

### 5.3 派生字段计算 (L308-L350)

| 派生字段 | 计算公式 |
|----------|----------|
| 开盘涨跌幅 | `(开盘价 - 昨收价) / 昨收价 × 100%` |
| 涨停价 | `昨收价 × 1.10` |
| 跌停价 | `昨收价 × 0.90` |
| vol0935Diff | `(当日09:35成交量 / 5分成交量250周期最大值) × 100` |
| volMax5MinDiff | `(当日最大5分钟成交量 / 5分成交量250周期最大值) × 100` |

---

## 六、免费数据源采集策略

**文件位置**: [StockDataService.java](file:///d:/BaiduNetdiskDownload/mootdx-java/javaTdx/src/main/java/com/example/tdx/service/StockDataService.java)

### 6.1 免费采集方案 (L232-L276)

```
步骤1: 使用新浪API批量查询
步骤2: 补充东方财富数据
  - 分批处理（默认50只/批）
  - 批次间延迟
  - 合并PE/PB/市值等字段
步骤3: 返回合并后的结果
```

### 6.2 东方财富数据补充 (L278-L342)

```
对于每批股票:
  1. 逐个查询东方财富API
  2. 合并PE、PB、总市值、流通市值、换手率
  3. 批次间延迟
  4. 记录成功/失败数量
```

---

## 七、策略对比总结

| 策略 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| 单数据源(SINA/EASTMONEY等) | 简单、快速 | 数据可能不完整 | 只需要基础行情 |
| FREE模式 | 免费、数据较全 | 需要多次API调用 | 免费获取较全数据 |
| FALLBACK模式 | 高可用、自动切换 | 可能较慢 | 需要高可靠性 |
| AGGREGATED-最快 | 响应快 | 数据可能不完整 | 追求响应速度 |
| AGGREGATED-最全 | 数据最完整 | 较慢、资源消耗大 | 需要完整数据 |
| AGGREGATED-智能 | 平衡速度和质量 | 复杂度较高 | 通用场景 |

---

## 八、参考文件清单

| 文件名 | 路径 |
|--------|------|
| StockDataService.java | javaTdx/src/main/java/com/example/tdx/service/StockDataService.java |
| FallbackStrategyService.java | javaTdx/src/main/java/com/example/tdx/service/datasource/FallbackStrategyService.java |
| AggregatedDataCollectorService.java | javaTdx/src/main/java/com/example/tdx/service/datasource/AggregatedDataCollectorService.java |
| DataMergeService.java | javaTdx/src/main/java/com/example/tdx/service/datasource/DataMergeService.java |

---

*文档生成完成*
